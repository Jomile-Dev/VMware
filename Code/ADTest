Get-ADUser -Filter {UserPrincipalName -eq "user@domain.com"} `
  -Server yourdcname `
  -Credential (Get-Credential) `  # enter the bind account creds here
  -Properties UserPrincipalName, sAMAccountName
