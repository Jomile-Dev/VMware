mkdir C:\Keys
ssh-keygen -t rsa -b 4096 -m PEM -f "esxi_key"

##SSH into ESXI
mkdir -p /etc/ssh/keys-root
chmod 700 /etc/ssh/keys-root

##Upload pub key
cat /tmp/esxi-key.pub >> /etc/ssh/keys-root/authorized_keys
chmod 600 /etc/ssh/keys-root/authorized_keys
rm /tmp/esxi-key.pub  
